package com.example.searc.repository;

import com.example.searc.model.PlaceLabel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlaceLabelRepository extends JpaRepository<PlaceLabel, Long> {
}