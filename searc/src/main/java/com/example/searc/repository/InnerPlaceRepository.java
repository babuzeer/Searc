package com.example.searc.repository;

import com.example.searc.model.InnerPlace;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InnerPlaceRepository extends JpaRepository<InnerPlace, Long> {
}